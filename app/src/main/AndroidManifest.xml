<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.socialnetworktwitch">

    <application
        android:name=".SocialNetworkApplication"
        android:allowBackup="true"
        android:icon="@drawable/globe"
        android:label="@string/app_name"
        android:roundIcon="@drawable/globe"
        android:supportsRtl="true"
        android:theme="@style/Theme.Socialnetworktwitch">
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Socialnetworktwitch.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>